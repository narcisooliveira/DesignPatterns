package diversos.questao_3.ordenacao;

public class insertion_sort extends abs_sort{
        // ordenando de forma crescente
        // laço com a quantidade de elementos do vetor - 1
    /*
    int eleito;
        for (int k = 0; k <= 4; k++) {
            eleito = x[k];
            j = k-1;

            // laço que percorre os elementos à
            // esquerda do número eleito
            // ou até encontrar a posição para
            // recolocação do número eleito
            // respeitando a ordenação procurada
            while (j >= 0 && x[j] > eleito){
                x[j+1] = x[j];
                j = j-1;
            }
            x[j+1] = eleito;
        }
        // mostrando o vetor ordenado
        for (int i = 0; i <= 4; i++) {
            System.out.println((i+1)+"número: "+x[i]);
        }
*/
    @Override
    public void ordenar(int[] valores) {

    }
}
